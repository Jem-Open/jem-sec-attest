# Docker local-stack environment variables.
# Copy to .env and fill in the required values before running `docker compose up`.
# See specs/011-docker-e2e-testing/quickstart.md for setup instructions.
#
# The Next.js application no longer runs in Docker. App configuration goes in .env.

# ── Required ─────────────────────────────────────────────────────────────────
DATABASE_URL=
SESSION_SECRET=
ACME_OIDC_CLIENT_SECRET=local-dev-secret-change-for-prod

# ── PostgreSQL (optional overrides — defaults shown) ─────────────────────────
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=jem_attest
