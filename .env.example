# Environment variables for jem-sec-attest
# Copy this file to .env and replace placeholder values with real secrets.
# NEVER commit .env to source control.

# Acme Corp tenant secrets
ACME_WEBHOOK_SECRET=replace-with-real-secret

# Globex Inc tenant secrets
GLOBEX_WEBHOOK_SECRET=replace-with-real-secret

# Session encryption secret (minimum 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=replace-with-a-minimum-32-character-secret

# AI Provider API Keys
# At least one is required for role profile generation
ANTHROPIC_API_KEY=your-anthropic-api-key-here
# OPENAI_API_KEY=your-openai-api-key-here
# AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here

# Sprinto compliance integration API keys (per tenant, optional)
# Required only for tenants with compliance.provider: "sprinto" in their YAML config.
# ACME_SPRINTO_API_KEY=your-sprinto-api-key-here
# GLOBEX_SPRINTO_API_KEY=your-sprinto-api-key-here

# Vercel AI Gateway URL (optional, defaults to direct provider access)
# AI_GATEWAY_URL=https://ai-gateway.vercel.sh/v1

# Database configuration
# If DATABASE_URL is set with a postgres:// or postgresql:// prefix, PostgreSQL is used.
# If not set, SQLite is used (default: data/jem.db).
#
# Examples:
# Self-hosted:       DATABASE_URL=postgres://user:pass@localhost:5432/jem_attest
# AWS RDS:           DATABASE_URL=postgres://user:pass@rds-host:5432/jem_attest?sslmode=require
# GCP Cloud SQL:     DATABASE_URL=postgres://user:pass@127.0.0.1:5432/jem_attest  (via Auth Proxy)
# Azure PostgreSQL:  DATABASE_URL=postgres://user:pass@azure-host:5432/jem_attest?sslmode=require
# Railway:           DATABASE_URL=postgres://user:pass@railway-host:5432/jem_attest?sslmode=require
# Neon:              DATABASE_URL=postgres://user:pass@neon-host:5432/jem_attest?sslmode=require
# Supabase:          DATABASE_URL=postgres://user:pass@supabase-host:5432/jem_attest?sslmode=require
# DATABASE_URL=postgres://user:pass@localhost:5432/jem_attest

# SQLite database path (used when DATABASE_URL is not set)
# DB_PATH=data/jem.db

# Application version for evidence stamping (optional)
# APP_VERSION=1.0.0
